# Com-M5.copyweb

This repository contains a simple HTML/CSS project. Automated tests are provided to verify the basic structure of the site.

## Running tests

Install dependencies and execute the test suite using `pytest`:

```bash
pip install beautifulsoup4 pytest
pytest
```

